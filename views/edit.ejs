<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

    <style>
        body {
            text-align: center;
        }
        img {
            max-width: 250px;
        }
        a {
            font-size: 30px;
        }
    </style>
    
    <a href="/pokemon/">Back to Pokedex</a> <br>

    <!-- Pokemon Name -->

    <h2> <%= poke.name %> </h2>

    <!-- Pokemon type -->

    <h3> <%= poke.type[0] %> </h3>
    <h3> <%= poke.type[1] %> </h3>

    <!-- Pokemon Id -->
 
    <p> #<%= poke.id %> </p> <br>

     <!-- Pokemon Image -->

     <img src="<%= poke.img %> " alt=""> <br><br>

    <!-- Main Stats -->

    <h4>Stats: </h4> 

    <p>hp:&nbsp<%= poke.stats.hp %> </p>
    <p>attack:&nbsp<%= poke.stats.attack %> </p>
    <p>defense:&nbsp<%= poke.stats.defense %> </p>
    <p>spattack:&nbsp<%= poke.stats.spattack %> </p>
    <p>spdefense:&nbsp<%= poke.stats.spdefense %> </p>
    <p>speed:&nbsp<%= poke.stats.speed %> </p>

<div>
    <h1>Edit <%= poke.name %>  page</h1>
    <form action="/pokemon/<%= index %>?_method=PUT" method="post">
        name: <input type="text" name="name" value="<%= poke.name %>" /> <br/>
        <input type="submit" value="Update <%= poke.name %>">
    </form>
</div>

</body>
</html>